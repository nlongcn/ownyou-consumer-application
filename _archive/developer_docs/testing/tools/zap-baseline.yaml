target:
  url: ${BASE_URL}
  recurse: true
  ajaxSpider: false
  include:
    - ${BASE_URL}/
    - ${BASE_URL}/analyses
    - ${BASE_URL}/recommendations
rules:
  ignore:
    - 10054 # Cookie Without SameSite Attribute (SPA, no server cookies expected)
    - 10096 # Timestamp Disclosure - Unix

